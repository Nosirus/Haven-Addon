name: "Haven"
description: "Self-hosted private chat (Discord alternative) - adapted for HA"
version: "1.0.2"
slug: "haven"
init: false
arch:
  - aarch64
  - amd64
ports:
  3000/tcp: 3000
map:
  - config:rw
options:
  server_name: ""
  admin_username: ""
schema:
  server_name: str
  admin_username: str
image: ghcr.io/nosirus/haven-addon-{arch}
